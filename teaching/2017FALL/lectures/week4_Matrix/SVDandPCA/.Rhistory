iris.data <- iris[,1:4]
ir.pca <- prcomp(iris.data,
center = TRUE,
scale. = TRUE)
ir.pca
names(ir.pca)
ir.pca$x
PC1 <- ir.pca$x[,"PC1"]
PC2 <- ir.pca$x[,"PC2"]
PC1
iris$Species
plot(PC1, PC2, color=iris$Species)
plot(PC1, PC2, col=iris$Species)
iris$Species
plot(PC1, PC2, col=iris$Species)
PC1
plot(PC1, PC2, col=iris$Species)
iris$Species
as.numeric(iris$Species)
levels(iris$Species)
biplot(ir.pca)
biplot(ir.pca)
ir.pca
names(ir.pca)
ir.pca$x
?prcomp
?biplot
class(ir.pca)
